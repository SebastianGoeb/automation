{
  "name": "automation",
  "version": "1.0.0",
  "description": "This repo contains all the automation for my personal projects.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "prettier": "^3.1.0"
  }
}
